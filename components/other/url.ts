const API_URL: string = "https://chatapp-r5ts.onrender.com";
const TWEETS_URL: string = `${API_URL}/tweets`;
const TWEET_URL: string = `${API_URL}/tweet`;
const LOGIN_URL: string = `${API_URL}/login`;
const SIGNUP_URL: string = `${API_URL}/signup`;
const USER_URL: string = `${API_URL}/user`;
const USERS_URL: string = `${API_URL}/users`;
export const FOLLOWERS_URL: string = `${API_URL}/followers`;
export const FOLLOWEES_URL: string = `${API_URL}/followees`;
export const FOLLOW_URL: string = `${API_URL}/follow`;

export { API_URL, TWEETS_URL, LOGIN_URL, SIGNUP_URL, USER_URL, TWEET_URL, USERS_URL };
